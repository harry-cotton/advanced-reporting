pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
pyarrow>=14            # parquet engine for the durable raw store (data/processed/history.parquet)
openpyxl>=3.1          # xlsx I/O for the naming generator + planner -> generator bridge
streamlit>=1.30

# --- Target MMM engine (heavy: TensorFlow Probability). Install separately when ready:
#       pip install google-meridian
# google-meridian>=1.1
