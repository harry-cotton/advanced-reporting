# Per-client context (GITIGNORED — nothing here enters git except this README)

One file set per client/engagement, written by a human. Copy
`client_brief.template.md` → `client_brief.md` and fill it in. `macro_notes.md`
holds curated external-context bullets (one per line, with a source) — the same
never-generated rule as always.

The agents read these alongside `../guidelines/` and the computed data summaries.
